# [Project] — Decisions

The ADR log: decisions that are closed, with the reasoning that closed them. **Check this file before reopening anything.** A decision reversed without a new ADR is not a decision, it is drift.

Format: what was decided, what it rules out, and what would justify revisiting it.

<!-- SKELETON. The template below is the format. Delete it once you have real ADRs.

     What belongs here:
     - Every decision the spec settled, with reasoning reconstructed from the spec
     - Every decision YOU made while building the corpus, clearly marked as yours
     - Every debate that closed later, added as it closes

     What does not:
     - Facts. "The database is Postgres" is a fact for the data-model file. "Postgres
       over Mongo because X" is an ADR.
     - Open questions. Those live in 12-risks.md until they close.

     Write the ADR especially when the decision looks obvious. An obvious decision with
     unrecorded reasoning is the one that gets quietly reversed six weeks later by
     someone who did not know it was a decision at all.

     Number sequentially and never renumber. A superseded ADR stays, with its status
     changed and a pointer to the one that replaced it — the log is a history, not a
     current-state document. -->

---

## ADR-001 — [The decision, as a statement]

**Date:** [YYYY-MM-DD] · **Status:** accepted <!-- accepted | superseded by ADR-nnn | open -->

[Why. State the forces that made this the answer — the constraint, the cost, the failure mode being avoided. Two or three sentences that would let a stranger reconstruct the argument.]

**Rules out:** [The alternatives that are now off the table, named specifically. This is what makes the ADR enforceable in review.]

**Revisit if:** [The concrete condition that would reopen this. If nothing could, say "this is permanent" and mean it.]

---
